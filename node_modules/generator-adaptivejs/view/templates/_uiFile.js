define(['$'], function($) {
    var <%= uiFunction %>UI = function() {
        // Add any scripts you would like to run on the <%= uiFunction %> page only here
    };

    return <%= uiFunction %>UI;
});
