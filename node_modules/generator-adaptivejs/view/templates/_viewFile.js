define([
    '$',
    '<%= parentViewFilePath %>',
    'dust!pages/<%= viewName %>/<%= viewName %>'
],
function($, <%= parentView %>, template) {
    return {
        template: template,
        extend: <%= parentView %>,

        context: {
            templateName: '<%= viewName %>',
            body: function() {
                return $('body');
            }
        }

        /**
         * If you wish to override preProcess/postProcess in this view, have a look at the documentation:
         * http://adaptivejs.mobify.com/v1.0/docs/views
         */
    };
});
