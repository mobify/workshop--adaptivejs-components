{?href}
    <a class="c-card__action {class}"
        {?id}id="{id}"{/id}
        href="{href}"
    >
        {body}
    </a>
{:else}
    <button class="c-card__action {class}"
        {?id}id="{id}"{/id}
        type="{?type}{type}{:else}button{/type}"
    >
        {body}
    </button>
{/href}
